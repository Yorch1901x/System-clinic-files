rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /inventario/{document} {
      allow read, write: if request.auth != null;
    }
    
    match /salida-inventario/{document} {
      allow read, write: if request.auth != null;
    }
  }
}
